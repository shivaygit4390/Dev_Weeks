// ONCE FUNCTION

// Goal:

// Function should run only once

// Example:
// init() → runs once
// init() → ignored next times

// Interview focus:

// closures + state persistence



// CUSTOM BIND (ADVANCED JS CORE)

// Goal:

// Implement Function.prototype.bind

// Interview focus:

// this + closures



// LOOP + CLOSURE EDGE CASES

// Practice:

// for (var i = 0; i < 5; i++) {
// setTimeout(() => console.log(i), i * 100);
// }

// Fix using:

// let
// closure
// bind


//you did leading thrittle that calls bw the delays are ignored write a code efor trailing throttle like the calls bw delay arent ignored they run and persists