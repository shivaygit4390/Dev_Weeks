

const PlayGround = () => {
  return (
    <div>Everything is in app jsx and this priject now follows a structured routing that itslef the plaground it is just teh message oplayground is nota seperate task instead it was to make thos entire week organized in a UI</div>
  )
}

export default PlayGround